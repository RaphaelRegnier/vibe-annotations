# v1.2.1

## Get Started Guide

New in-toolbar guide walks you through setup in 4 steps: install the server, annotate, copy to your agent, and optionally connect via MCP. Tabbed configs for Claude Code, Cursor, Windsurf, Codex, and OpenClaw — each with a one-click copy button.

## Reorganized Settings

Settings menu reordered for faster access: Get started, Documentation and Release notes are now at the top. Server status, pin color, and toggle options follow below.

## README Updates

Added Codex (TOML config) and OpenClaw (JSON config) setup instructions to the README alongside existing agents.
